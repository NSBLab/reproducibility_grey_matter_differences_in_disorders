<?xml version="1.0" encoding="UTF-8"?><documentation release="21a"><function><title>cor_var_age</title><syntaxes><syntax>[varTable, meanAge, stdAge, nSite] = cor_var_age(metadata, diagnosisString, corTmap1, siteList, iter, type)</syntax></syntaxes><purpose>Analyze correlation of age-related differences in t-map data across sites</purpose><description>&lt;span&gt;&lt;p&gt;&lt;br/&gt;INPUT:&lt;br/&gt;metadata         : A structure containing metadata about subjects, including:&lt;br/&gt;- metadata.diagnosis_string: Diagnosis labels (e.g., 'HC', 'P').&lt;br/&gt;- metadata.site_string: Site labels for each subject.&lt;br/&gt;- metadata.age: Age of each subject.&lt;br/&gt;diagnosisString  : A string or array of strings specifying the diagnoses of interest.&lt;br/&gt;corTmap1         : Correlation matrix of t-maps (nSite x nSite).&lt;br/&gt;iter             : Number of iterations for the Mantel test.&lt;br/&gt;type             : Type of correlation to compute ('pearson', 'spearman', etc.).&lt;br/&gt;&lt;br/&gt;OUTPUT:&lt;br/&gt;varTable         : Table summarizing Mantel test results for age differences.&lt;br/&gt;nSite            : Number of unique sites in the dataset.&lt;br/&gt;&lt;br/&gt;DESCRIPTION:&lt;br/&gt;This function calculates age statistics (mean and standard deviation) for each site,&lt;br/&gt;focusing on subjects within the specified diagnoses and healthy controls ('HC').&lt;br/&gt;It then computes the correlation between these age statistics and the provided&lt;br/&gt;correlation matrix of t-maps using a helper function (`cal_var_age`).&lt;br/&gt;&lt;br/&gt;STEPS:&lt;br/&gt;1. Identify the rows in the metadata corresponding to the specified diagnoses (`LaDiag`).&lt;br/&gt;2. Extract unique site identifiers and count the number of sites (`nSite`).&lt;br/&gt;3. For each site, calculate:&lt;br/&gt;- Mean age of subjects (`age(iSite,1)`).&lt;br/&gt;- Standard deviation of age (`age(iSite,2)`).&lt;br/&gt;4. Call `cal_var_age` to compute correlations and generate the results table.&lt;br/&gt;&lt;br/&gt;NOTES:&lt;br/&gt;- Relies on the `cal_var_age` function for computing correlations and returning results.&lt;br/&gt;- Ensures statistics are computed only for subjects belonging to the specified diagnoses&lt;br/&gt;or healthy controls ('HC').&lt;br/&gt;&lt;br/&gt;EXAMPLE USAGE:&lt;br/&gt;[varTable, nSite] = cor_var_age(metadata, {'P'}, corTmap1, 1000, 'spearman');&lt;/p&gt;&lt;/span&gt;</description><descriptionText>
INPUT:
metadata         : A structure containing metadata about subjects, including:
- metadata.diagnosis_string: Diagnosis labels (e.g., 'HC', 'P').
- metadata.site_string: Site labels for each subject.
- metadata.age: Age of each subject.
diagnosisString  : A string or array of strings specifying the diagnoses of interest.
corTmap1         : Correlation matrix of t-maps (nSite x nSite).
iter             : Number of iterations for the Mantel test.
type             : Type of correlation to compute ('pearson', 'spearman', etc.).

OUTPUT:
varTable         : Table summarizing Mantel test results for age differences.
nSite            : Number of unique sites in the dataset.

DESCRIPTION:
This function calculates age statistics (mean and standard deviation) for each site,
focusing on subjects within the specified diagnoses and healthy controls ('HC').
It then computes the correlation between these age statistics and the provided
correlation matrix of t-maps using a helper function (`cal_var_age`).

STEPS:
1. Identify the rows in the metadata corresponding to the specified diagnoses (`LaDiag`).
2. Extract unique site identifiers and count the number of sites (`nSite`).
3. For each site, calculate:
- Mean age of subjects (`age(iSite,1)`).
- Standard deviation of age (`age(iSite,2)`).
4. Call `cal_var_age` to compute correlations and generate the results table.

NOTES:
- Relies on the `cal_var_age` function for computing correlations and returning results.
- Ensures statistics are computed only for subjects belonging to the specified diagnoses
or healthy controls ('HC').

EXAMPLE USAGE:
[varTable, nSite] = cor_var_age(metadata, {'P'}, corTmap1, 1000, 'spearman');</descriptionText></function></documentation>